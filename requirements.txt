# DevMind AI - runtime dependencies
# Python 3.13+

# Configuration
python-dotenv>=1.2,<2.0

# Local inference. Foundry Local serves an OpenAI-compatible API on the
# loopback interface, so the official client is all that is needed; the
# foundry-local-sdk package only adds service lifecycle management, which
# this application deliberately leaves to the user.
openai>=2.50,<3.0

# Document parsing
pypdf>=6.14,<7.0
python-docx>=1.2,<2.0
markdown>=3.10,<4.0

# User interface
streamlit>=1.60,<2.0
